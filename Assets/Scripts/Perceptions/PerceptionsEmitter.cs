using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Component in charge of computing the perceptions and sending them to SARL using the SARL interface
/// </summary>
public class PerceptionsEmitter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
